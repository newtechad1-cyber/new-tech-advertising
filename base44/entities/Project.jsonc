{
  "name": "Project",
  "type": "object",
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "data.company_id": "{{user.data.client_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  },
  "properties": {
    "company_id": {
      "type": "string",
      "description": "Reference to Company ID"
    },
    "proposal_id": {
      "type": "string",
      "description": "Reference to Proposal ID"
    },
    "name": {
      "type": "string",
      "description": "Project name"
    },
    "project_type": {
      "type": "string",
      "enum": [
        "ada_rebuild",
        "website_rebuild",
        "streaming_tv",
        "video_production",
        "social_setup",
        "other"
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "not_started",
        "in_progress",
        "review",
        "ready_to_launch",
        "completed",
        "on_hold"
      ],
      "default": "not_started"
    },
    "assigned_to": {
      "type": "string",
      "description": "Team member email responsible"
    },
    "start_date": {
      "type": "string",
      "format": "date"
    },
    "due_date": {
      "type": "string",
      "format": "date"
    },
    "completed_date": {
      "type": "string",
      "format": "date"
    },
    "progress": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "default": 0
    },
    "milestones": {
      "type": "string",
      "description": "JSON string of milestone steps and statuses"
    },
    "deliverable_urls": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Links to delivered work"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "company_id",
    "name",
    "project_type"
  ]
}