import React from 'react';

/**
 * Intentionally retired from the homepage conversion flow.
 *
 * The former section repeated trust, human-judgment, and relationship ideas
 * already communicated in the hero, process, founder, and supporting pages.
 * Keeping this component as a no-op avoids breaking existing imports while the
 * homepage is reviewed and shortened.
 */
export default function PhilosophySection() {
  return null;
}
