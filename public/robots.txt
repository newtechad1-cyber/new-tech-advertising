User-agent: *
Allow: /

# Internal/private HTML routes are intentionally crawlable so search engines
# can see their page-level noindex directives and remove legacy URLs from the
# index. They remain excluded from the public sitemap and public route inventory.
# Do not use robots.txt as an indexing control for these pages.

Sitemap: https://newtechadvertising.com/sitemap.xml
